# Phase 5 Build Progress

Full task details in `docs/phase5-plan.md`. Phase 4 context in `docs/phase4-plan.md` + `docs/phase4-progress.md`. Parked Phase 6 (Reports & Commissions) lives at `docs/phase6-plan.md` + `docs/phase6-progress.md`.

**Status legend**: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Phase P — Catalog foundations

- [x] **P1** — Products schema + zod (`products` table, audit `entityType` enum extended, `productSchema` / `createProductSchema` / `updateProductSchema`). Depends on: Phase 4 complete.
- [x] **P2** — Products API (scope-aware CRUD + archive, audit each mutation, mount in `index.ts`). Depends on: P1.
- [x] **P3** — Products UI (list, create, edit, archive; sidebar + mobile nav entries; works on 375px). Depends on: P2.

## Phase Q — Stock movements ledger

- [x] **Q1** — Stock movements schema + ledger service (`stock_movements` table, `services/stock.ts` with `getStockLevel`, `recordMovement`, `voidMovement`). Depends on: P1.
- [x] **Q2** — Stock movements API (list / record / void, sign validation per type). Depends on: Q1.
- [x] **Q3** — Stock levels endpoint + products list integration (real qty + low-stock badge). Depends on: Q2.
- [x] **Q4** — Movement entry UI (sheet/dialog from product detail; receive / sell / adjust / transfer). Depends on: Q3.
- [x] **Q5** — Product detail with movement history (per-distributor stock for admin, history table, void). Depends on: Q4.

## Phase R — Polish & integration

- [x] **R1** — Inventory dashboard widget + `/inventory` alias route. Depends on: Q5.
- [x] **R2** — CSV exports for inventory (`/products.csv`, `/stock-movements.csv`). Depends on: R1.
- [x] **R3** — Seed updates (8–12 products, ~30 movements including 1 voided). Depends on: Q5.

---

## Notes / Decisions Log

(Add entries here when blocking or deviating from the plan. Format: `YYYY-MM-DD — task — note`.)

- 2026-05-24 — pivot — Phase 5 changed from Reports & Commissions to Inventory. Original Phase 5 plan parked as Phase 6 (`docs/phase6-plan.md`, `docs/phase6-progress.md`). Inventory ships as a standalone module — receivables stay text-only; receivable ↔ product wiring is deferred.
- 2026-05-24 — plan — 9 tasks across P (catalog) / Q (stock ledger) / R (polish). Locked: distributor-scoped products, per-distributor unique SKU, integer-unit qty, ledger-style movements (sign per type, void replaces edit, negative stock allowed), audit events `product.*` + `stock.*`, low-stock threshold fixed at 5, no receivable wiring.
- 2026-05-24 — P2 — Products API mounted at `/api/products`. SKU conflict handled via pre-insert SELECT inside the tx → `HTTPException(409)` (no DB-error parsing). Distributor users get `distributorId` auto-resolved from session; admin must pass it. Update path omits SKU per plan. Archive is idempotent (returns row unchanged if already archived, no double audit). List endpoint returns rows shaped to `productListItemSchema` with `currentQty: 0` placeholder — Q3 wires the real number via separate stock-levels endpoint.
- 2026-05-24 — P1 — `products` table added with `(distributorId, sku)` UNIQUE + `(distributorId, status)` indexes. `auditEvents.entityType` enum extended with `product` + `stock_movement` (added now so Q1 needs no second migration). Audit event-type enum gained `product.*` + `stock.*` events. Zod schemas in `packages/schema/src/product.ts`. `db:push` + `typecheck` green.
- 2026-05-24 — Q1 — `stock_movements` table added with `(distributorId, productId, createdAt)` + `(productId, voidedAt)` indexes. `referenceType` / `referenceId` nullable (forward hook). `recordedByUserId` typed as `text` (no FK to `user` — matches `visits.recordedByUserId` pattern). Stock service (`apps/api/src/services/stock.ts`) owns all qty math: `signQty(type, qty)` (receive/transfer_in → `+|qty|`, sale/transfer_out → `-|qty|`, adjustment → qty as-is), `getStockLevel(tx, productId, distributorId)`, `getStockLevels(client, { distributorId?, productIds? })` (grouped by product+distributor), `recordMovement` (asserts product active, scope, signs qty, audit), `voidMovement` (idempotent — second void returns row unchanged with no audit). Zod schemas in `packages/schema/src/stock.ts` (`stockMovementSelectSchema`, `recordMovementSchema` with `superRefine` for sign rule, `voidMovementSchema`, `stockLevelSchema`, plus query schemas for Q2/Q3). `stockMovementTypeEnum` added to `enums.ts`. `db:push` + `db:seed` + `typecheck` green.
- 2026-05-24 — Q2 — Stock movements API mounted at `/api/stock-movements`. `listMovements` added to `services/stock.ts` (joins products/distributors/user for `stockMovementListItemSchema` rows; admin can filter via `?distributorId=`, distributor scoped via `scope.filterQuery`; `includeVoided` defaults false; `from`/`to` parsed as `Date`). Route is chained (`GET /`, `POST /`, `POST /:id/void`); sign-per-type validation lives in `recordMovementSchema.superRefine` so zValidator returns 400 on negative qty for non-adjustment types. Void route unwraps `{ reason }` to keep service signature unchanged. `typecheck` green.
- 2026-05-25 — Q3 — Stock-levels endpoint added at `GET /api/products/stock-levels` (mounted before `/:id` so the static path wins). `listStockLevels` in `services/products.ts` fetches scoped products, calls `getStockLevels` from the stock service with their IDs, then merges so every in-scope product gets a row (`currentQty` defaults to 0 when no movements exist). Distributor scope applied via `Scope.filterQuery`; admin honours optional `distributorId` filter. Web: `useStockLevels` hook (key `["products","stock-levels"]`) + `StockBadge` (low ≤5 outline, ≤0 destructive). Products data table injects a `stockByProduct` Map via TanStack `meta`; mobile card list reads the same map. `typecheck` green.
- 2026-05-25 — Q3 — simplify pass: collapsed `listStockLevels` to a single `LEFT JOIN stock_movements ON (productId, distributorId, voidedAt IS NULL) GROUP BY product.id` (kills the two-query pattern, composite-key merge Map, redundant `Number()` cast, and the `(SQL | undefined)[]` filter guard). Dropped `export` on `LOW_STOCK_THRESHOLD`; collapsed `StockBadge` to a single Badge with a computed variant; extracted `<StockCell qty>` so the table cell and mobile card share one path. Removed the `useMemo` around `stockByProduct` per the no-premature-memoization rule. Skipped: cross-cutting `useApiQuery` / `throwIfNotOk` helpers, `scope.distributorFilter` extraction, and `TableMeta` module augmentation (out of Q3 scope).
- 2026-05-25 — Q5 — Product detail extracted to `features/products/product-detail.tsx`; `$id.tsx` now only resolves the route param and renders `<ProductDetail />`. Detail composes header (name, SKU, status, price) + Receive/Adjust/Edit/Archive buttons + stock summary (admin = `Distributor stock` card with `StockBadge`; distributor = big tabular number + `StockBadge`) + movement history (desktop table, mobile card list — `useIsMobile`). History uses `useMovements(productId)` with `includeVoided=true` so voided rows render with a `VOID` badge + void reason. `useProductStockLevels(productId)` was added to `queries.ts` (key `[...stockLevelsKey, productId]`) so stock here refreshes independently of the global levels list. Void action lives in an `AlertDialog` with a reason `Textarea` — Action stays disabled until reason is non-empty; second void of the same row is a service-side no-op (already idempotent from Q1). Date formatting via `Date#toLocaleString` (matches `visits/visit-list.tsx`). `typecheck` green.
- 2026-05-25 — R3 — Seed updates shipped. 11 products (6 metro: Bed/Dining/Office chair/Sofa/Side table + archived Bunk bed; 5 qc: Dresser/Coffee table/Wardrobe/Recliner/Bar stool), mix of priced + 2 unpriced (Side table, Bar stool). 27 stock movements over the last 60 days (10 receive / 12 sale / 3 adjustment / 1 transfer_in / 1 transfer_out). Trimmed to plan's 10–12 sale ceiling by combining duplicate sales into single rows. 1 voided sale on the qc Dresser (`voidedAt` + `voidReason` set inline at insert — no separate UPDATE pass). `createdAt` set explicitly per movement via a `daysAgo(n)` helper so movement timestamps land in the last 60 days. `recordedByUserId` resolved by fetching admin + dist user rows after `seedAuth.signUpEmail` and using them per movement (admin records qc, dist records metro). Added `stockMovements` / `products` to the deletion sequence in reverse-FK order. Final stock checks (excluding archived): one out-of-stock (Dining table @ -1), one low-stock (Wardrobe @ 4), rest healthy. Hit an unrelated DB blocker mid-task: `local.db` was on a pre-Phase-5 schema (missing `products` / `stock_movements`) and `drizzle-kit push` reported "Changes applied" without actually creating the tables (libsql/turso driver appears to no-op on certain mid-migration states). Resolution: `rm local.db local.db-{shm,wal} packages/db/local.db` then re-ran `db:push` — fresh DB picked up every table cleanly. Filed mentally as "if a Phase-5 push acts like a no-op, reset the SQLite file." `db:push` + `db:seed` + `typecheck` green.
- 2026-05-25 — R2 — simplify pass: dropped `toIso` helper from `routes/exports.ts`, inlined `.toISOString()` since drizzle returns `Date` (`createdAt` non-null, `voidedAt` nullable → `r.voidedAt?.toISOString() ?? null`). Extracted internal `ExportButton({ href, filename, label })` base inside `features/exports/export-buttons.tsx`; the 4 button components now compose it. Skipped: reusing `stockMovementQuerySchema` (forces coupling to a 500-row-capped schema with `limit` / `includeVoided` semantics that don't apply to exports), removing the `Record<string, unknown>[]` cast (needed for TS index-signature compat; matches existing `customers.csv` / `overdue.csv` pattern), and aligning service param shape with `StockMovementQuery` (low value, would re-introduce the limit-cap coupling). `typecheck` green.
- 2026-05-25 — R2 — Inventory CSV exports shipped. `PRODUCTS_CSV_COLUMNS` + `STOCK_MOVEMENTS_CSV_COLUMNS` (snake_case keys matching existing `OVERDUE_CSV_COLUMNS` / `CUSTOMER_CSV_COLUMNS` style) added to `packages/schema/src/csv.ts` along with `ProductCsvRow` / `StockMovementCsvRow` types. Services: `listProductsForExport` in `services/products.ts` (scope-aware, INNER JOIN distributors, LEFT JOIN non-voided `stock_movements` with `coalesce(sum(qty),0)` for `currentQty`, admin honours optional `distributorId`, sort by `products.id desc`, 10 000 cap); `listMovementsForExport` in `services/stock.ts` (separate from `listMovements` so it bypasses the zod-capped `limit: 500` and includes voided rows by default — audit completeness per plan; same scope + filter shape as the API list route, sort by `stock_movements.id desc`). Routes `GET /api/exports/products.csv?distributorId=` and `GET /api/exports/stock-movements.csv?productId=&distributorId=&from=&to=` mounted; query params validated by inline `z.cuid2()` / `z.string()` schemas. `toIso` helper converts `Date | string | null` → ISO for `created_at` / `voided_at` (dates land as `Date` from drizzle SQLite timestamp_ms). Web: `ProductsExportButton` + `ProductMovementsExportButton({ productId })` added to `features/exports/export-buttons.tsx` (anchor + `buttonVariants({ variant: "outline" })`, matches existing pattern); wired into `product-list.tsx` toolbar and `product-detail.tsx` header. Distributor scope already enforced server-side. `bun run --filter '*' typecheck` green.
- 2026-05-25 — R1 — Inventory dashboard widget + `/inventory` alias landed. `stockMovementQuerySchema.limit` (1–500) added; `listMovements` applies it (default 1000). `useRecentMovements(limit=5)` added to `features/products/queries.ts` (key `movementKeys.recent(limit)`); `useRecordMovement` invalidation widened to `movementKeys.all` so the dashboard list refreshes too. `inventory-summary.tsx` renders 3 KPI cards (Active products / Low stock 0 < qty ≤ 5 / Out of stock qty ≤ 0) plus a Recent movements card (latest 5, product → `/products/$id`, signed +qty / -qty colouring); when there are zero products the recent card is replaced by an "Add product" CTA. KPI counts filter stock levels by the active-product id set so archived rows never inflate the counters. `LOW_STOCK_THRESHOLD` exported from `stock-badge.tsx` so the widget and the badge agree on the same threshold (5). `dashboard.tsx` mounts `<InventorySummary />` between the receivables KPI cards and the chart. `/inventory` route is a `beforeLoad` redirect → `/products` (matches the `today`/`audit` redirect pattern). Out-of-scope deviation: fixed `to="/products/$id_/edit"` → `to="/products/$id/edit"` in `product-detail.tsx` + `products-data-table.tsx` — the `tanstack-start` route-tree regen this task triggered emits a `declare module '@tanstack/react-start' { Register … }` augmentation that wasn't in HEAD; that augmentation tightens Link's `to` typing and surfaced two pre-existing typos. `bun run --filter '*' typecheck` green.
- 2026-05-25 — Q4 — Movement entry UI shipped. `useRecordMovement` + `useVoidMovement` added to `features/products/queries.ts` (invalidate products list + `stockLevelsKey` + `movementKeys.list(productId)`; `movementKeys.all` is reserved for Q5 history reads). `stock-movement-form.tsx` is a controlled Dialog wrapping a RadioGroup (Receive/Sale/Adjustment/Transfer in/Transfer out) + numeric qty input + reason textarea; an inline +/− toggle appears only for `adjustment`, and the form sends a signed qty (`-parsed` when minus) — non-adjustment types submit the positive integer and the server signs it. `reasonNote` is required only for adjustment. `useEffect` resets `type` from `defaultType` whenever `open` flips so the parent can preset between Receive/Adjust without remounting. `$id.tsx` now exposes Receive/Adjust quick-action buttons that drive the shared Dialog (both disabled when the product is archived). `typecheck` green.
- 2026-05-24 — P3 — Products UI shipped: TanStack-Query hooks (`productQueries` + `useArchiveProduct`), shadcn data table (Active/Archived tabs, search by name/SKU, columns with `—` stock placeholder per Q3), TanStack-Form `ProductForm` (admin distributor combobox on create, SKU disabled on edit, price input in pesos → cents on submit), routes `/products`, `/products/new`, `/products/$id`, `/products/$id_/edit`, sidebar "Inventory" group + mobile bottom nav "Products" entry (visible to both roles). Row archive uses confirm AlertDialog. Route tree regenerated via `@tanstack/router-cli generate`. `typecheck` green.
