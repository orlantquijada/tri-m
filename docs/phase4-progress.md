# Phase 4 Build Progress

Full task details in `docs/phase4-plan.md`. Phase 3 context in `docs/phase3-plan.md` + `docs/phase3-progress.md`.

**Status legend**: `[ ]` todo · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Phase J — Customer Intake & Distributor Onboarding Flow

- [x] **J1** — Multi-step customer intake wizard (3 steps: Contact → Location → Risk & notes)
- [x] **J2** — Interactive map picker for customer location (Leaflet pin, tap/drag/clear, reverse-geocode via Nominatim proxy)

## Phase K — Visit & Promise Tracking

- [x] **K1** — Visits schema + record-visit dialog (visits table, route, dialog, profile integration)
- [x] **K2** — Open promises surface (unresolved promises card, one-tap resolve)
- [x] **K3** — Customer activity timeline (chronological feed merging receivables / payments / visits / status changes)

## Phase L — Distributor Field UX (mobile-responsive)

- [x] **L1** — Distributor "Today" page (4 sections: overdue, promises, due today, recent visits)
- [x] **L2** — Mobile-responsive layout polish (ResponsiveTable, bottom nav, sticky action bar, ≥44px tap targets)
- [x] **L3** — Inline quick-actions on customer / overdue rows (Call, SMS, Map, Record payment, Record visit)

---

## Notes / Decisions Log

(Add entries here when blocking or deviating from the plan. Format: `YYYY-MM-DD — task — note`.)

- 2026-05-18 — plan — Phase 4 plan written. 8 tasks across 3 mini-phases (J–L). Theme: distributor field workflow — customer intake, visit/promise tracking, mobile-responsive UX. Locked: visits are insert-only (corrections via follow-up visit); promise-to-pay is a column set on visits, not a separate table; promise resolution is explicit (distributor toggles `promiseResolvedAt`), not auto-derived; "Today" uses browser local time; mobile-responsive only (no PWA / no offline / no file uploads — those deferred to Phase 5). No new audit events for visits in Phase 4 (visits are their own audit trail). Order: wizard → duplicate resolver → visits schema → open promises → timeline → today page → mobile polish → quick actions.
- 2026-05-18 — plan — Added J3 (interactive map picker for customer location). Now 9 tasks total (J: 3, K: 3, L: 3). Locked: Leaflet + OSM tiles (no key); Nominatim for reverse geocode via server-side proxy at `/api/geocode/reverse` with 1 req/sec token-bucket rate limit and proper `User-Agent` (Nominatim policy); pin-based only (no forward address search in Phase 4); reverse-geocode prefills Address field only when blank; submit succeeds even if Nominatim errors out. Raw lat/lng inputs land in J1 first (Location step uses existing form fields), then J3 swaps them for the map picker — keeps J1 small and ships J3 as an independent deliverable.
- 2026-05-18 — plan — Deferred J2 (smart duplicate-phone resolution) out of Phase 4. Phase 2 yellow-banner warning is enough for now. Renamed old J3 (map picker) → J2 to keep the J-phase contiguous. Phase 4 now 8 tasks total (J: 2, K: 3, L: 3).
- 2026-05-18 — J2 — Map picker: dynamic-import Leaflet in `useEffect` (mirrors `map-view.tsx`, avoids SSR crash). Click drops/moves marker; marker draggable with 1s debounce on `dragend` before reverse-geocode fires. "Use my location" recenters + drops pin. "Clear" removes marker + nulls form lat/lng. Reverse-geocode auto-fills Address when blank; else shows "Use suggested: …" link. Nominatim proxy at `/api/geocode/reverse` validates lat/lng, sends `User-Agent` (`tri-m-furniture-collections/1.0`) + `Accept-Language: en`, returns `{ data: null }` on rate-limit miss / network error so client never blocks submit. Token bucket: 1 token / sec, in-memory module-level. `customer-form.tsx`: removed raw lat/lng `Input`s + `validateLatitude/Longitude` + `captureLocation` helper — picker owns all of that. Default center: Manila (14.5995, 120.9842), zoom 13.
- 2026-05-18 — K1 — Visits table added with FKs to customers + distributors (distributorId denormalized for fast scoping), `recordedByUserId` text FK to better-auth user. Indexes: `(customerId, createdAt)` and `(distributorId, promiseResolvedAt, promisedDate)` for open-promise lookup. `visitInsertSchema` Zod refine enforces both `promisedAmountCents` + `promisedDate` required iff `outcome === "promised"`, both null otherwise. Service applies `Scope.forUser(user).assertCanWrite(customer.distributorId)` on create and `filterQuery(visits.distributorId)` on list. `resolvePromise` relies on distributor scope only (no per-user recorder check — distributor scope sufficient per plan). Routes: `GET /visits` (scoped, with optional `customerId` + paging), `GET /visits/open-promises` (added now to back K2), `POST /visits`, `PATCH /visits/:id/resolve-promise`. Web: `RecordVisitDialog` uses RadioGroup for type + outcome, conditional promise amount/date inputs when outcome=promised, "Use my location" GPS button (visit saves without coords if skipped). `VisitList` shows colored badge per outcome, inline "Mark resolved" button on open promises, `geo:` + Google Maps links when GPS present. Sidebar gains "Visits" entry for both roles. Cross-customer log at `/visits` shows ID-linked rows (no joined customer name in payload — kept simple; can extend later). db:push ran after killing dev server (pid 68878) holding the SQLite lock; drizzle-kit recreated multiple existing tables to normalize default expressions — visits table created alongside. db:seed ran clean.
- 2026-05-19 — K2 — `listOpenPromises` extended to inner-join `customers` (id, fullName, phone, address) and project `daysUntilDue` (negative if overdue) computed server-side via JS `Date` in the server's local TZ — `promisedDate` parsed as `YYYY-MM-DD` against `startOfLocalDay(new Date())`, rounded to whole days. Route `GET /visits/open-promises` already added in K1; payload shape now nests `customer` and adds `daysUntilDue`. Web: `OpenPromisesCard` renders Card → list rows with name link to `/customers/$id`, formatted peso amount, promised date, tone-pill (red `overdue`, amber `today`/`soon`, emerald `later`), and `Resolved` button. One-tap resolve is implemented as a row-local optimistic hide: while `resolveMutation.isPending || isSuccess`, the row returns `null` so it disappears immediately; cache invalidation in `useResolvePromiseMutation` (already from K1, prefix `visitKeys.all`) refetches the list. Errors re-render the row with destructive message. No schema change. typecheck green.
- 2026-05-19 — L1 — `getToday` runs 5 parallel queries: overdue count+sum (receivables where `firstDueDate < date('now')` and `currentBalanceCents > 0`), due-today count+sum (`paymentSchedules` joined to `receivables` for scope, `dueDate = date('now')`, status pending|partial), open-promises count, recent visits (last 24h via `createdAt >= unixepoch('now','-1 day')*1000`, joined to customer for name/phone, limit 20), and due-today rows (same filter as the agg, projected with customer fields, limit 50). Plan called for only counts + recentVisits in the payload, but the "Due today" section needs row data with inline action; extended payload with `dueToday: ScheduleRow[]` rather than minting a new endpoint. Overdue section reuses existing `useOverdueQuery` (slice top 5) so the Today page composes from 2 endpoints (`/dashboard/today` + `/overdue`) + open-promises. Plan also listed `apps/web/src/routes/_authed/index.tsx` as the role-aware redirect — that path collides with the unauthed `routes/index.tsx` (both match `/`), so role decision lives in `routes/index.tsx` instead (reads `authClient.getSession()`, redirects distributor→`/today`, admin→`/dashboard`). Sidebar gains "Today" as first item for both roles. typecheck green; no schema change.
- 2026-05-19 — K3 — Timeline service merges 5 sources in parallel (`Promise.all`): receivables, payments (split into `recorded` + optional `voided`), visits (split into `recorded` + optional `promise.resolved`), audit `customer.status_changed` rows for the customer entityId, and blacklist requests (split into `requested` + optional `approved`/`rejected` from the row's own status — avoids joining audit by request id). Distributor scope enforced once via `Scope.forUser(user).assertCanRead(customer.distributorId)` before any fan-out queries. `payments.voidedAt` is text-ISO so parsed via `new Date()` with NaN guard. Event payload is `Record<string, unknown>` — client-side renderer dispatches via name→fn map (`RENDERERS[event.type]`) to keep complexity ≤20. `customer-profile.tsx` keeps the structured Receivables + Visits sections (data-dense, already there) and appends a new "Activity" section under them — chose append over replace to minimize blast radius on existing pages; future polish task can collapse the duplication. Visit + payment mutations now invalidate `customerKeys.timeline(customerId)` alongside the existing detail key. Added `customerKeys.timeline(id) = [...customerQueries.keys.detail(id), "timeline"]` so the helper is colocated. typecheck green; pre-existing ultracite errors unchanged (no new ones from K3).
- 2026-05-19 — L2 — `ResponsiveTable` is config-driven (`columns: { key, header, cell, mobileLabel?, mobileHidden?, mobilePrimary?, className?, headerClassName? }[]` + `data` + `keyExtractor`), renders Shadcn `<Table>` at `md:` and a stack of Shadcn `<Card>`s with a `<dl>` of key:value pairs below. Optional `mobileFooter(row)` slot lets callers inject action buttons (used by overdue card to show Map + Visit). Adopted in `customer-list.tsx` and `overdue-table.tsx`. `customer-profile.tsx`: header turned into a sticky bar (`sticky top-14 z-20`, backdrop-blur) with horizontal-scroll action row on mobile, vertical-stack title block above. `receivable-detail.tsx`: collapsed the two separate summary grids into one card with a 2×4 mobile / 4-col desktop top row + 1-col/2-col/3-col detail grid below, and wrapped `PaymentHistory` in `overflow-x-auto`. Mounted `MobileBottomNav` inside `_authed.tsx` rather than `__root.tsx` (plan said root, but root holds the unauthed shell with no `SidebarProvider`; bottom-nav needs `useSidebar`, so authed layout is the correct host); `<main>` gets `pb-20 md:pb-4` so content clears the nav. Bottom-nav 4th icon is `Menu` (opens existing Shadcn mobile-sidebar `Sheet` via `setOpenMobile(true)`) instead of plan-named "Profile" — there is no profile route; the menu pattern gives mobile users one-tap access to the full sidebar (admin extras, sign-out, user info). Plan's "Record payment" sticky action skipped on customer profile — `PaymentForm` needs a `receivableId`, and a customer-level picker is out of scope (record-payment still lives on `receivable-detail`). `payment-history.tsx` not modified (file not in plan); horizontal scroll wrapper at the parent keeps the existing table usable on narrow viewports. Shadcn `Button` from base-ui has no `asChild` — used `buttonVariants` + `cn` on `<a>` for the Map link button. typecheck green.
- 2026-05-20 — L3 — `QuickActionsBar` lives at `apps/web/src/features/shared/quick-actions-bar.tsx`. Renders Call/SMS/Map/Visit/Payment as 44px icon buttons, two layouts (`row` for mobile horizontal-scrollable strip + `wrap` for desktop right-aligned column). Call+SMS hidden when phone is null/empty (not disabled, per acceptance); Map hidden when either coord null; Record Payment hidden when no `receivableId`. Map href flips between `geo:` (mobile, detected via `matchMedia("(max-width: 768px)")` in `useEffect`) and `mapsUrl(lat,lng)` (desktop). `PaymentForm` gained a `trigger?: React.ReactElement` prop mirroring `RecordVisitDialog` pattern; existing receivable-detail callsite unchanged (default trigger kept). Deviation from plan file list: server projections in `apps/api/src/services/dashboard.ts` extended (`topOverdue` + `dueToday` rows gain `phone/latitude/longitude`; `recentVisits` gains `phone` only — visit rows in Today don't show actions, but `phone` reserved for future Call from visit row). `listOpenPromises` in `apps/api/src/services/visits.ts` extended (nested `customer` gains `latitude/longitude`; `phone` already projected by K1). Hono RPC re-infers all client types automatically — no schema file edits. `customer-list.tsx`: new actions column (desktop) + `mobileFooter` slot for `row` layout; customer rows have no `receivableId` → Payment hidden. `overdue-table.tsx`: dedicated Map column folded into single Actions column (avoids duplicate Map button in row + footer). `today-view.tsx`: OverdueSection + DueTodaySection rows now use `QuickActionsBar` in `row` layout via existing `TodayListRow.action` slot; `RecentVisitsSection` left alone (no actions on visit rows). `open-promises-card.tsx`: bar appended below the Resolved button cluster — deviation from plan file list (file not in L3 plan, but the card sits on Today page and benefits from the bar; user-confirmed full-set scope). `e.stopPropagation()` on bar container guards against future row-level click handlers. typecheck green; no schema change → no `db:push`.
- 2026-05-18 — J1 — Wizard ships 3 steps (Contact / Location / Risk & notes). Refactored `customer-form.tsx` to export `useCustomerForm`, `CustomerFormApi`, `CustomerFormValues`, `ContactFields`, `LocationFields`, `RiskFields`, and `buildCustomerPayload`. `CustomerForm` keeps its single-form variant (edit page untouched). Wizard composes the three field groups in order; per-step `Next` calls `form.validateField(name, "change")` for each field in the current step and uses `form.Subscribe` to derive a reactive `canAdvance` flag (disables Next/Submit when required fields are empty or any current-step field errors). Final step renders `<RiskFields />` plus a read-only review summary of name/phone/address/coords sourced from `form.Subscribe`. Sales-channel skipped — not on customer schema (lives on receivables). Mobile: step indicator hides labels under `sm:`, Back/Cancel/Next stack column-reverse under `sm:`.
